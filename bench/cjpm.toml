[package]
  cjc-version = "1.0.5"
  name = "bench"
  description = "matcang performance benchmarks (harness + suites)"
  version = "1.0.0"
  output-type = "executable"
  compile-option = "-O2"

[dependencies]
  matcang = { path = "../" }
